# はじめに

TODO:要推敲
- コーヒーには様々な抽出方法がある。コーヒー豆を専門店で買ってきて自宅で入れる場合に味に影響する条件は、温度や抽出時間など様々だがコーヒーを挽く細かさも大きな要因となる。
- コーヒーロースターのイメージする最高の抽出に近づけようとしたらメッシュの調整は欠かすことができない
- コーヒー粉の長径は重要な要素ではあるが、長径の平均値が一致する粉体の分布で同じ味になるとは限らない
- 分布の形状や粉体の形などもコーヒーの味に大きく影響がある
- しかし、コーヒーの粉体の大きさや分布はコーヒーの味の調整に取って非常に有用な情報だ
- このような統計情報を手軽に得ることができればコーヒーロースターが想う最高の抽出に自宅で近づく手がかりになる //控えめな表現すぎる。もう少し大きく言って良いと思う（有用さが局所的すぎるのは良くない）

# GrindGuide のモデリングについてChatGPTとの会話

以下の点を話し合った（https://chatgpt.com/share/69d350ef-6328-83a8-a416-fd76ea184bc8）
- C4モデルについて
- まずどのように設計するか

要約
- FastAPI + Fiji実装スケルトンを先に進め、初期実装を進める。
- C4文章版を維持しながら設計を具体化する
- FastAPIを利用したスケルトン構造を考えてもらった
- 初期検証で越えるべき壁の進め方を検討してもらった
  - Calibration 単体検証
  - headless Fiji 単体検証



## ChatGPATの推奨する構成


```
powder_service/
├─ app/
│  ├─ main.py
│  ├─ config.py
│  ├─ routes/
│  │  ├─ analyze.py
│  │  └─ compare.py
│  ├─ services/
│  │  ├─ calibration.py
│  │  ├─ fiji_runner.py
│  │  ├─ measurement_parser.py
│  │  ├─ normalization.py
│  │  ├─ kde_stats.py
│  │  └─ bundle.py
│  ├─ models/
│  │  ├─ requests.py
│  │  └─ responses.py
│  ├─ utils/
│  │  ├─ files.py
│  │  └─ logging.py
│  └─ tmp/
├─ macros/
│  └─ measure_particles.ijm
├─ tests/
│  ├─ test_calibration.py
│  ├─ test_measurement_parser.py
│  └─ test_kde_stats.py
├─ samples/
│  ├─ jpeg/
│  └─ csv/
├─ requirements.txt
└─ README.md
```

## 依存モジュール

requirements.xt

```
fastapi
uvicorn[standard]
python-multipart
pydantic
pandas
numpy
scipy
matplotlib
opencv-python
```


# 初期検証

## calibration の検証

- プログラム的なテストはOK
- 自動化で得られる計測の精度的にはFijiの測定との相対誤差は0.4%でスケールに当てはめると0.002mm
- コーヒーの粉体の測定は長径0.5程度より大きくなると思うのでこの誤差は問題なさそう。
- 十分な実用範囲内の誤差でスケール（px_per_mm）を返すことが期待できる
- 速度は微妙に待つ感じでちょっと遅いのだが、これは画像の大きさに依存するとのこと
- calibration はここで一旦固定（2026/4/9）

## headless Fiji の検証

- fiji_runnerの確認
  - 固定画像に対して　```--headless -macro``` でCSVが出力されえるか確認 > 空のCSVは返る
- Fijiの操作を箇条書きでcodexに渡してmacroにしてもらったので検証を行う（2026/4/11）
  - CODEXでチャットしながらでバグを行なった。基本エラーコードや出力を渡して修正してもらっている。最終的な出力のフォーマットは渡している。
- CODEXで色々チャットしながらheadless FijiのAnalyze particlesのでバグ
  - 環境をubuntuに移行（）
  - 移行にあたってGithubにGrindGideレポジトリを置く。
  - 色々行うが同じエラーが出続ける
  - xvfb-runを使うことを提案されるがそもそもapt-get installに失敗する
- claudeにも相談する（解決策の提案内容がclaudeの方がより本質的な気がする）（2026/4/11）
  - https://claude.ai/share/58a50169-a9d7-415d-8154-8e9573d010f7
  - 何をしてもjava.lang.NullPointerExceptionが消えない。
  - BatchModeがよくなさそうだがmacroからだと　setBatchMode(false);　が効かない -> js macro版をJython版に置き換える提案 > OK。採択する。
  - Jython版はijモジュールを使うのでFiji appの中からrunする感じになる？
  - GLIBC2.31で動くFijiを入れ直す（最新版はGLIBC2.34が必要だがubuntu20は2.31まで対応）
    - https://downloads.imagej.net/fiji/archive/20220414-1745/fiji-linux64.zip
    - Fijiのテストコード
```
/usr/local/bin/Fiji.app/ImageJ-linux64 --headless --console \
  -macro /mnt/meta-dog/GrindGuide/service/app/measure_particles.ijm \
  "image=/mnt/meta-dog/GrindGuide/service/tests/IMG_7066.jpg;output=/mnt/meta-dog/GrindGuide/service/tests/out.csv;threshold_min=80;threshold_max=255;px_per_mm=54"
```
割とすんなり計測できるので時間がかかるのはcalibrationの部分なのだなという予想。

- 上記calibrationを読んでいない、Fiji実行だけのコードなのでCODEXに一本化してもらう。
実行は下記でOK
```
export FIJI_EXECUTABLE=/usr/local/bin/Fiji.app/ImageJ-linux64
export FIJI_HEADLESS=true
python service/app/test_fiji_runner.py \
  --input service/tests/IMG_7066.jpg \
  --output service/tests/out.csv \
  --threshold-min 80 \
  --threshold-max 255

```

### ubuntuに移行 引き継ぎのドキュメントをCODEXに書いてもらった。
- testを書いておいたら引き継ぎで勝手にテストを実行してくれた。
- 引き継ぎの段階で環境が無いのでmicromanbaで作る（huskyはcondaが壊れた？）
